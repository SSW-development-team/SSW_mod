LFF_policy = {
	invade_another_american_countries = {
		icon = generic_operation
		available = {
			has_completed_focus = LFF_resumption_of_domestic_affairs
			has_army_manpower = { size > 299999 }
		}
		visible = {
			has_completed_focus = LFF_resumption_of_domestic_affairs
			FROM = {
				exists = yes
				OR = {
					original_tag = BLS
					original_tag = LUI
					original_tag = OKL
					original_tag = TEX
					original_tag = USA
				}
			}
		}
		target_trigger = {
			FROM = {
				exists = yes
				OR = {
					original_tag = BLS
					original_tag = LUI
					original_tag = OKL
					original_tag = TEX
					original_tag = USA
				}
			}
		}
		cost = 50
		fire_only_once = no
		ai_will_do = { factor = 10 }
		days_remove = 30
		remove_effect = {
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}
	}
	comply_with_the_agreement = {
		icon = generic_operation
		available = {
			is_in_faction = no
			has_completed_focus = LFF_dealing_with_the_devil
			NOT = { has_war_with = FROM }
			any_other_country = {
				has_war_with = GER
				has_war_with = LFF
			}
		}
		visible = {
			is_in_faction = no
			has_completed_focus = LFF_dealing_with_the_devil
			NOT = { has_war_with = FROM }
			any_other_country = {
				has_war_with = GER
				has_war_with = LFF
			}
			FROM = {
				exists = yes
				is_faction_leader = yes
				original_tag = GER
			}
		}
		target_trigger = {
			FROM = {
				exists = yes
				is_faction_leader = yes
				original_tag = GER
			}
		}
		cost = 50
		fire_only_once = yes
		ai_will_do = { factor = 10 }
		complete_effect = {
			FROM = { country_event = { id = ssw_lff.14 } }
		}
	}
}
